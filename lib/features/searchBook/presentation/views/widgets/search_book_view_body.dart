import 'package:flutter/material.dart';

class SearchBookViewBody extends StatelessWidget {
  const SearchBookViewBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}